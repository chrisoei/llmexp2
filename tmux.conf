set -g default-terminal "screen-256color"
set -g status-right "docker"

set -g window-status-current-style bg=red
set -g window-status-style bg=green
set -g status-style fg=black,bg=colour102

set -g prefix C-k
unbind C-b
bind C-k send-prefix

